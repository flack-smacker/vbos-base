/** A software simulation of a memory management unit. **/
/** Created by Joe Muro on 9/30/13. **/

function MMU(memory) {

    this.memory = memory; // A reference to main memory hardware.
    this.

    this.write = function(address, byte) {

    };

    this.read = function(address, offset, nBytes) {

    };

    this.allocate = function(nBytes) {

    };

    this.deallocate = function(rangeStart, nBytes) {

    };
}

